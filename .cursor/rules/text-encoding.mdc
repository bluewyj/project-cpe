---
description: 含中文的文档与源码必须 UTF-8；禁止未显式 UTF-8 的 PowerShell 与对含中文文件的 StrReplace
alwaysApply: true
---

# 文本编码（防中文乱码）

适用范围：`.md` / `.mdc`，以及**含中文注释或中文字符串**的源码（如 `.rs` / `.ts` / `.tsx` / `.js` / `.jsx` 等）。

## 硬约束（必须遵守）

- 上述文件必须是 **UTF-8（无 BOM）**
- **禁止**用 Windows PowerShell 的 `Get-Content` / `Set-Content` / `Out-File` 在**未显式指定 UTF-8**时改写这些文件（中文系统默认 GBK，会把 UTF-8 多字节尾字节弄成 `?`）
- **禁止**对本仓库**含中文**的上述文件使用 Cursor `StrReplace`（本机曾反复写出非法 UTF-8）；只允许下列安全写法
- 纯 ASCII 源码文件可用 `StrReplace`；一旦改动触及中文注释/字符串，整文件按下面方式写回

## 唯一允许的改法

1. Cursor **`Write` 整文件覆盖**（内容一次性写全）
2. **Python**（推荐）：`Path(path).write_text(text, encoding="utf-8", newline="\n")`
3. **Node**：`fs.writeFileSync(path, text, { encoding: "utf8" })`
4. 若必须用 PowerShell，读写都要显式 UTF-8 无 BOM：
   - 读：`Get-Content -Path $f -Raw -Encoding utf8`
   - 写：`[IO.File]::WriteAllText($f, $text, [Text.UTF8Encoding]::new($false))`
   - **禁止**依赖默认编码的 `Set-Content` / `Out-File`

局部小改也须：先读入内存 → 改字符串 → 再整文件写回（不要 `StrReplace`）。

## 改完自检（未通过视为未完成）

- 能严格 `utf-8` 解码（无 `UnicodeDecodeError`）
- 不得出现 U+FFFD
- 警惕模式：UTF-8 三字节序列末字节变成 ASCII `?`（`0x3F`）
