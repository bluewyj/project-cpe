# UDX710 / project-cpe — Cursor 规则索引

项目 AI 约定在 `.cursor/rules/*.mdc`（优先）。此处不重复长篇技术说明。

| 规则 | 何时生效 |
|------|----------|
| `00-project-stack.mdc` | 常驻：技术栈、目录、硬约束 |
| `text-encoding.mdc` | 常驻：UTF-8；禁乱码写法（含 Shell/HEREDOC） |
| `rust-backend.mdc` | `backend/**/*.rs` |
| `dbus-ofono.mdc` | D-Bus / AT / 串行相关 |
| `frontend-react.mdc` | `frontend/**/*.{ts,tsx}` |
| `api-feature-flow.mdc` | 增改 HTTP API |

协作方式、Git、版本、真机验收见根目录 `AGENTS.md`。  
若与本文件冲突，**以 `.mdc` 与 `AGENTS.md` 为准**。
